import 'package:flutter/material.dart';

class TodolistViewer extends StatelessWidget {
  const TodolistViewer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder(
      color: Colors.yellow,
    );
  }
}
